diff a/server/db/models/user.js b/server/db/models/user.js	(rejected hunks)
@@ -1,3 +1,3 @@
-const crypto = require('crypto')
-const Sequelize = require('sequelize')
-const db = require('../db')
+const crypto = require('crypto');
+const Sequelize = require('sequelize');
+const db = require('../db');
@@ -19 +19 @@ const User = db.define('user', {
-      return () => this.getDataValue('password')
+      return () => this.getDataValue('password');
@@ -27 +27 @@ const User = db.define('user', {
-      return () => this.getDataValue('salt')
+      return () => this.getDataValue('salt');
@@ -51 +51 @@ const User = db.define('user', {
-      return `${this.firstName} ${this.lastName}`
+      return `${this.firstName} ${this.lastName}`;
@@ -61 +61 @@ const User = db.define('user', {
-})
+});
@@ -67,2 +67,2 @@ User.prototype.correctPassword = function (candidatePwd) {
-  return User.encryptPassword(candidatePwd, this.salt()) === this.password()
-}
+  return User.encryptPassword(candidatePwd, this.salt()) === this.password();
+};
@@ -74,2 +74,2 @@ User.generateSalt = function () {
-  return crypto.randomBytes(16).toString('base64')
-}
+  return crypto.randomBytes(16).toString('base64');
+};
@@ -82,2 +82,2 @@ User.encryptPassword = function (plainText, salt) {
-    .digest('hex')
-}
+    .digest('hex');
+};
@@ -90,2 +90,2 @@ const setSaltAndPassword = (user) => {
-    user.salt = User.generateSalt()
-    user.password = User.encryptPassword(user.password(), user.salt())
+    user.salt = User.generateSalt();
+    user.password = User.encryptPassword(user.password(), user.salt());
@@ -93 +93 @@ const setSaltAndPassword = (user) => {
-}
+};
@@ -98,3 +98,3 @@ User.beforeCreate((user) => {
-    .toLowerCase()}`
-  user.firstName = properFirst
-})
+    .toLowerCase()}`;
+  user.firstName = properFirst;
+});
@@ -105,3 +105,3 @@ User.beforeCreate((user) => {
-    .toLowerCase()}`
-  user.lastName = properLast
-})
+    .toLowerCase()}`;
+  user.lastName = properLast;
+});
@@ -109,2 +109,2 @@ User.beforeCreate((user) => {
-User.beforeCreate(setSaltAndPassword)
-User.beforeUpdate(setSaltAndPassword)
+User.beforeCreate(setSaltAndPassword);
+User.beforeUpdate(setSaltAndPassword);
@@ -112,2 +112,2 @@ User.beforeBulkCreate((users) => {
-  users.forEach(setSaltAndPassword)
-})
+  users.forEach(setSaltAndPassword);
+});
@@ -115 +115 @@ User.beforeBulkCreate((users) => {
-module.exports = User
+module.exports = User;
