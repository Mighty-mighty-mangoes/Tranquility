diff a/server/api/users.spec.js b/server/api/users.spec.js	(rejected hunks)
@@ -3,5 +3,5 @@
-const {expect} = require('chai')
-const request = require('supertest')
-const db = require('../db')
-const app = require('../index')
-const User = db.model('user')
+const {expect} = require('chai');
+const request = require('supertest');
+const db = require('../db');
+const app = require('../index');
+const User = db.model('user');
@@ -11,2 +11,2 @@ describe('User routes', () => {
-    return db.sync({force: true})
-  })
+    return db.sync({force: true});
+  });
@@ -15 +15 @@ describe('User routes', () => {
-    const codysEmail = 'cody@puppybook.com'
+    const codysEmail = 'cody@puppybook.com';
@@ -19,3 +19,6 @@ describe('User routes', () => {
-        firstName: 'cody', lastName: "pug", email: 'cody@puppybook.com', password: 'bones',
-      })
-    })
+        firstName: 'cody',
+        lastName: 'pug',
+        email: 'cody@puppybook.com',
+        password: 'bones',
+      });
+    });
@@ -24,3 +27 @@ describe('User routes', () => {
-      const res = await request(app)
-        .get('/api/users')
-        .expect(200)
+      const res = await request(app).get('/api/users').expect(200);
@@ -28,5 +29,5 @@ describe('User routes', () => {
-      expect(res.body).to.be.an('array')
-      expect(res.body[0].email).to.be.equal(codysEmail)
-    })
-  }) // end describe('/api/users')
-}) // end describe('User routes')
+      expect(res.body).to.be.an('array');
+      expect(res.body[0].email).to.be.equal(codysEmail);
+    });
+  }); // end describe('/api/users')
+}); // end describe('User routes')
